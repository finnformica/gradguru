export type DataProps = {
  title: string;
  desc: string;
  tags: string;
};

export type titleDataProps = { title: string };

export type addFormData = {
  title: string;
  summary: string;
  tags: string;
};
